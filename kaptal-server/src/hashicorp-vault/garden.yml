kind: Module
name: vault
type: container
image: vault
services:
  - name: vault
    ports:
      - name: http
        containerPort: 3005
    ingresses:
     - path: /
       port: http
       hostname: vault.${var.base-hostname}