kind: Module
type: container
name: users-mongodb
image: mongo
description: MongoDB for users-service. Contains users data
services:
  - name: users-mongodb
    volumes:
      - name: data
        containerPath: /data/db
    ports:
      - name: mongodb
        containerPort: 27017