kind: Module
type: container
name: mongo-db
image: mongo
services:
  - name: mongo-db
    volumes:
      - name: data
        containerPath: /data/db
    ports:
      - name: mongodb
        containerPort: 27017