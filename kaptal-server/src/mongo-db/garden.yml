kind: Module
type: container
name: redis
services:
  - name: redis
    ports:
      - name: http
        containerPort: 3006
    ingresses:
     - path: /
       port: http
       hostname: redis.${var.base-hostname}