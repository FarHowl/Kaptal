kind: Project
name: kaptal
modules:
   include:
     - kaptal-server
     - kaptal-client
environments:
  - name: default
providers:
  - name: local-kubernetes
    environment: [local]
    context: minikube
