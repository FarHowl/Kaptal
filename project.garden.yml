kind: Project
name: kaptal
modules:
   include:
     - kaptal-server
     - kaptal-client
     - mongo-db
     - hashicorp-vault
     - mongo-cache
environments:
  - name: default
providers:
  - name: local-kubernetes
    environment: [local]
    context: minikube
