const jwt = require("jsonwebtoken");

const verifyJWT = async (req) => {
    // const token = req.headers.authorization.split(" ")[1];

    // jwt.verify(token, process.env.publicKeyForUsers);
};

module.exports = verifyJWT;
